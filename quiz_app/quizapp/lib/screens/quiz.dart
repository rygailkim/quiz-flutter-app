import 'package:flutter/material.dart';

class QuizScreen extends StatelessWidget {
  const QuizScreen({Key? key, required String quizId}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
